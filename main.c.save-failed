#include "indice-invertido.h"
//#include "Historia.txt"

int main(){

    char vocab[TEXTO_LEN][MAX_PAL];

    carregarArquivo(vocab, NOME_ARQUIVO);

    int i = 0;
    while(i<1100){
        printf("%s\n", vocab[i]);
        i++;
    }

    return 0;
}
